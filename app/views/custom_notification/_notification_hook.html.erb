<script type="text/javascript">
  function selectCustomNotificationTemplate(url, select) {
    $.ajax({
      url: url,
      type: 'get',
      data: { custom_notification_template_id: $(select).val() }
    });
  }
</script>

<%= link_to_if_authorized(image_tag('email.png', :plugin => 'redmine_custom_notification', :style => 'vertical-align: middle;') + ' ' + l(:field_mail_notification),
                          { :controller => 'custom_notification', :action => 'new', :issue_id => @issue },
                          :remote => true,
                          :method => 'get',
                          :tabindex => 200) %>
